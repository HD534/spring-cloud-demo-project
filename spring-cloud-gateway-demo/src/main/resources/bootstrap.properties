
# use this config to get properties from config-server
#need to name as bootstrap.properties

spring.application.name=gateway
spring.profiles.active=production
spring.cloud.config.uri=http://localhost:8888